# Backend

Simple Express backend using TypeScript.

## Available scripts

- `npm run build` &mdash; compile TypeScript to `dist/`
- `npm start` &mdash; run compiled server
- `npm run dev` &mdash; start development server with live reload (`ts-node-dev`)

## Quick start

```bash
cd apps/backend
npm install
npm run dev
```